const address = require('./address/address.js')
const contact = require('./contact/contact.js')
const email   = require('./email/email.js')
const load    = require('./load/load.js')
const phone   = require('./phone/phone.js')
const quote   = require('./quote/quote.js')

const schemas = {
    // Address: address,
    // Contact: contact,
    // Email: email,
    // Load: load,
    // Phone: phone,
    Quote: quote
}

module.exports = schemas